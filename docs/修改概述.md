加了邮箱注册功能
将ui直接改为手机适配
端口换为3003
激活 proxy.ts，使 /r/:meetingId 成为可分享的用户报名入口；未登录访问时携带 callbackUrl 跳转登录，登录后返回报名页
登录/注册页支持 callbackUrl，实现管理员后台与普通用户报名入口分流
后台会议列表增加「复制报名链接」，方便管理员复制分享
新增用户跨会议报名记录页面 /admin/users/[id]/registrations，支持查询同一账号在多个会议的报名情况
后台侧边栏将「会议管理」置于首位，未选择会议前其它菜单项禁用；进入后台先跳转到 /admin/meetings
将后台大量的文字链接操作改为按钮样式交互（编辑、删除、返回、导出等统一为按钮）
