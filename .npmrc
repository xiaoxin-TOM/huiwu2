# @lamberl-lee/file-preview@0.8.x 的 peer 依赖自相矛盾：主包要 pdfjs-dist@^4.4.0，
# 而它的可选 peer @aiden0z/pptx-renderer 要 pdfjs-dist@>=5 <7。两者无法同时满足，
# npm 默认的严格 peer 解析会直接失败。锁定 pdfjs-dist@4（主包声明的版本）并放宽解析。
# 该库升级后应复查此项是否还有必要。
legacy-peer-deps=true
